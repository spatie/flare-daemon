#!/usr/bin/env php
<?php

declare(strict_types=1);

$pharPath = dirname(__DIR__).'/build/daemon.phar';

if (! is_file($pharPath)) {
    fwrite(STDERR, "Missing build/daemon.phar. Run `bash build.sh` from the flare-daemon repository.\n");
    exit(1);
}

require $pharPath;
